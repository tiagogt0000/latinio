import {sortedCollections,refreshDecks,deckMembers,pendingMembers} from './refresh-decks.js';
export function refreshList(data,h){return `<section class="card"><div class="button-row"><button class="button primary" data-action="refresh-edit">Neue Auffrisch-Sammlung</button><button class="button secondary" data-action="check-picker">Kann ich sie noch?</button></div><p class="small muted">Eigene Übungslisten mit Verweisen auf deine Wörter. Keine Kopien, keine doppelten Suchtreffer. Vollständig gewusste Wörter sind erledigt.</p>${refreshDecks(data).map(d=>{const total=deckMembers(data,d).length,pending=pendingMembers(data,d).length;return `<article class="refresh-row"><label class="collection-activity"><span><strong>${h(d.name)}</strong><small>${pending} offen · ${total-pending} erledigt</small></span><input class="switch" type="checkbox" data-refresh-active="${h(d.id)}" ${d.active!==false?'checked':''} aria-label="${h(d.name)} aktiv lernen"></label><div class="button-row"><button class="text-button" data-action="refresh-practice" data-id="${h(d.id)}" ${!pending||d.active===false?'disabled':''}>Üben</button><button class="text-button" data-action="refresh-edit" data-id="${h(d.id)}">Bearbeiten</button><button class="text-button" data-action="refresh-check" data-id="${h(d.id)}">Kann ich sie noch?</button><button class="text-button" data-action="refresh-restart" data-id="${h(d.id)}">Erneut üben</button><button class="text-button danger-text" data-action="refresh-delete" data-id="${h(d.id)}">Löschen</button></div></article>`;}).join('')||'<p class="muted">Noch keine Auffrisch-Sammlung. Erstelle eine manuell oder nach einem Auffrischungstest.</p>'}</section>`;}
export function checkPicker(data,h){return `<h2 id="modal-title">Auffrischen – kann ich sie noch?</h2><p class="muted">Wähle eine oder mehrere Lektionen. Jedes Wort kommt einmal dran. Danach kannst du die unsicheren Wörter in einer Auffrisch-Sammlung speichern.</p><form id="check-picker-form"><div class="bounded-list">${sortedCollections(data).map(c=>`<label class="collection-check"><input type="checkbox" name="lesson" value="${h(c.id)}"><span>${h(c.name)}</span></label>`).join('')}</div><button class="button primary wide" type="submit">Test starten</button></form>`;}
export function refreshEditor(data,{id='',selected=[],name='',merge=false},h){
 const deck=data.settings[id],chosen=new Set(deck&&!merge?deckMembers(data,deck).map(m=>m.wordId):selected);
 const words=sortedCollections(data).flatMap(c=>Object.values(data.words).filter(w=>w.collectionId===c.id));
 return `<h2 id="modal-title">${id?'Auffrisch-Sammlung bearbeiten':'Wörter zum Auffrischen'}</h2><form id="refresh-form" data-id="${h(id)}" data-merge="${merge}">${merge?`<label>Speichern in<select name="target"><option value="">Neue Auffrisch-Sammlung</option>${refreshDecks(data).map(d=>`<option value="${h(d.id)}">${h(d.name)}</option>`).join('')}</select></label>`:''}<label id="refresh-name-label">Name<input name="name" maxlength="100" value="${h(deck?.name||name||'Meine Auffrisch-Sammlung')}" required></label><p class="small muted">Wähle die Wörter aus. Auch richtig beantwortete Wörter kannst du hinzufügen. Gespeicherte Originale und Lernstände bleiben erhalten.</p><input type="search" id="refresh-word-search" placeholder="Wörter oder Lektion filtern" aria-label="Wörter filtern"><div class="bounded-list refresh-word-picker">${words.map(w=>`<label class="collection-check" data-refresh-word-row><input type="checkbox" name="word" value="${h(w.id)}" ${chosen.has(w.id)?'checked':''}><span><strong>${h(w.latin)}</strong><small>${h(w.meanings.map(g=>g.join(' / ')).join(', '))} · ${h(data.collections[w.collectionId].name)}</small></span></label>`).join('')}</div><p class="small" id="refresh-selected-count">${chosen.size} Wörter ausgewählt</p><button class="button primary wide" type="submit">Auswahl speichern</button></form>`;
}
